# RStudio / R cruft
.Rproj.user
.Rhistory
.RData
.Ruserdata

# Quarto project cache (NOT the rendered site — those .html / site_libs / _freeze
# files are committed on purpose because GitHub Pages serves them directly).
/.quarto/

**/*.quarto_ipynb

# Private / internal — kept locally, NOT published to the public repo
/CLAUDE.md
/How-to-Add-a-Blog-Post.md
/How-to-Add-a-Blog-Post.docx
/_source/
/.claude/
